<header>
    <div class="col-xs-6">
        <h2>Picu haldur <span class="badge">{{ version }}</span></h2>
    </div>
    <div class="col-xs-6 text-right">
        <div class="btn-group">
            <a href="{{ url_for('logout') }}" class="btn btn-danger">
                {{ user.username }}
                <i class="glyphicon glyphicon-off"></i>
            </a>
        </div>
    </div>
    <div class="clearfix"></div>
</header>
